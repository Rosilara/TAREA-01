const nombres = (prompt('Ingrese su nombre:'));
const apellidos = (prompt('Ingrese su apellido'));

console.log(nombres.concat(' ').concat(apellidos));
alert("SU NOMBRE COMPLETO ES: " + nombres.concat(' ').concat(apellidos))
