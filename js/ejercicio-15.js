
const contraseñaCorrecta = "unah2021curoc";

const verificación = prompt("Ingrese su contraseña:");

if (verificación === contraseñaCorrecta) {
  alert("Contraseña correcta acceso permitido");
} else {
  alert("Contraseña incorrecta");
}